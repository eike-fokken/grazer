### Version file ###
# changes trigger the Release Workflow when pushed to master
# cf. .github/workflows/release.yml

# Remember to update the CHANGELOG.md (-> will become the Release description)
set(GRAZER_VERSION 0.2.4)
