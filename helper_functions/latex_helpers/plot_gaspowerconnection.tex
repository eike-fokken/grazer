\documentclass{standalone}

%\providecommand{\datapath}{.}

\usepackage{tikz}
\usepackage{pgfplotstable}
\usepackage{siunitx}
\pgfplotsset{compat=1.13}
\usetikzlibrary{plotmarks,calc,math,shapes}

\begin{document}
\begin{tikzpicture}
  \begin{axis}[
    x filter/.code={\pgfmathparse{#1/3600}\pgfmathresult},
    xlabel={\large t(\si{\hour})},
    ylabel={\large p(\si{\bar})},
    table/col sep=comma,
    legend pos=north east,legend cell align=left]

    % Note that the first output at t=0 is skipped as this is just the initial condition.
    % It may be physically unsound.
    \pgfplotstableread[skip first n =2]{../../g2p_ld29_N7061_flow.csv}\flow
    \pgfplotstableread[skip first n =2]{../../g2p_ld29_N7061_pressure.csv}\pressure
    \addplot[color=cyan,solid,line width=1pt] table[ x index =0,y index=1] {\flow};
    \addplot[color=red,solid,line width=1pt] table[x index =0,y index=1] {\pressure};
    \addlegendentry{\tiny Flux}
    \addlegendentry{\tiny Pressure}
      \end{axis}
\end{tikzpicture}
\end{document}


